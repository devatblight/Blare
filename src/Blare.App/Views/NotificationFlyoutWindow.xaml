<Window
    x:Class="Blight.Blare.App.Views.NotificationFlyoutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- The window itself is the card. No inner Border: the window already has
         a surface (acrylic) and a frame, so wrapping the content in another
         bordered panel just draws a box inside a box. -->
    <Grid
        x:Name="RootHost"
        Padding="16,14"
        Background="{ThemeResource BlareStripBackground}"
        ColumnSpacing="13"
        Opacity="0">

        <Grid.RenderTransform>
            <TranslateTransform x:Name="CardTransform" />
        </Grid.RenderTransform>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Border
            x:Name="AccentStripe"
            Grid.Column="0"
            CornerRadius="2"
            Background="{ThemeResource BlareAccent}" />

        <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
            <TextBlock
                x:Name="TitleText"
                FontSize="13.5"
                FontWeight="SemiBold"
                TextWrapping="NoWrap"
                TextTrimming="CharacterEllipsis" />
            <TextBlock
                x:Name="MessageText"
                FontSize="12"
                Opacity="0.75"
                TextWrapping="Wrap"
                MaxLines="3"
                TextTrimming="CharacterEllipsis" />
        </StackPanel>

        <Button
            x:Name="ActionButton"
            Grid.Column="2"
            VerticalAlignment="Center"
            Visibility="Collapsed"
            Click="OnActionClicked" />

        <Grid.Resources>
            <Storyboard x:Name="EnterStoryboard">
                <DoubleAnimation
                    Storyboard.TargetName="RootHost"
                    Storyboard.TargetProperty="Opacity"
                    From="0" To="1" Duration="0:0:0.22">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation
                    Storyboard.TargetName="CardTransform"
                    Storyboard.TargetProperty="Y"
                    To="0" Duration="0:0:0.28">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>

            <Storyboard x:Name="ExitStoryboard">
                <DoubleAnimation
                    Storyboard.TargetName="RootHost"
                    Storyboard.TargetProperty="Opacity"
                    To="0" Duration="0:0:0.16">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseIn" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
        </Grid.Resources>
    </Grid>
</Window>
