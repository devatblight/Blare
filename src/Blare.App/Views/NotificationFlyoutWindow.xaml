<Window
    x:Class="Blight.Blare.App.Views.NotificationFlyoutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid x:Name="RootHost" Padding="10">
        <Border
            x:Name="Card"
            Padding="16,14"
            CornerRadius="10"
            Background="{ThemeResource BlareStripBackground}"
            BorderBrush="{ThemeResource BlareStripBorder}"
            BorderThickness="1"
            Opacity="0"
            PointerEntered="OnPointerEntered"
            PointerExited="OnPointerExited">

            <Border.Shadow>
                <ThemeShadow />
            </Border.Shadow>

            <Border.RenderTransform>
                <TranslateTransform x:Name="CardTransform" />
            </Border.RenderTransform>

            <Grid ColumnSpacing="13">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border
                    x:Name="AccentStripe"
                    Grid.Column="0"
                    Width="3"
                    CornerRadius="2"
                    Background="{ThemeResource BlareAccent}" />

                <StackPanel Grid.Column="1" Spacing="3" VerticalAlignment="Center">
                    <TextBlock
                        x:Name="TitleText"
                        FontSize="13.5"
                        FontWeight="SemiBold"
                        TextWrapping="Wrap" />
                    <TextBlock
                        x:Name="MessageText"
                        FontSize="12"
                        Opacity="0.75"
                        TextWrapping="Wrap" />
                </StackPanel>

                <Button
                    x:Name="ActionButton"
                    Grid.Column="2"
                    VerticalAlignment="Center"
                    Visibility="Collapsed"
                    Click="OnActionClicked" />
            </Grid>
        </Border>

        <!-- Slide and fade, cubic ease-out. Animating the card rather than the
             window keeps it GPU-composited and avoids moving the HWND. -->
        <Grid.Resources>
            <Storyboard x:Name="EnterStoryboard">
                <DoubleAnimation
                    Storyboard.TargetName="Card"
                    Storyboard.TargetProperty="Opacity"
                    From="0" To="1" Duration="0:0:0.22">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation
                    x:Name="EnterSlide"
                    Storyboard.TargetName="CardTransform"
                    Storyboard.TargetProperty="Y"
                    To="0" Duration="0:0:0.28">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>

            <Storyboard x:Name="ExitStoryboard">
                <DoubleAnimation
                    Storyboard.TargetName="Card"
                    Storyboard.TargetProperty="Opacity"
                    To="0" Duration="0:0:0.16">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseIn" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
        </Grid.Resources>
    </Grid>
</Window>
