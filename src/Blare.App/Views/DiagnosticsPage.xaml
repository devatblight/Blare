<Page
    x:Class="BLight.Blare.App.Views.DiagnosticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid Padding="16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Diagnostics" FontSize="20" FontWeight="SemiBold" />
                <TextBlock
                    Text="Everything Blare is doing, updated live. Read-only."
                    FontSize="12"
                    Opacity="0.6" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <ToggleSwitch
                    x:Name="LiveToggle"
                    OnContent="Live"
                    OffContent="Paused"
                    IsOn="True"
                    Toggled="OnLiveToggled" />
                <Button Content="Copy" Click="OnCopyClicked" />
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1">
            <StackPanel x:Name="SectionsPanel" Spacing="10" />
        </ScrollViewer>
    </Grid>
</Page>
